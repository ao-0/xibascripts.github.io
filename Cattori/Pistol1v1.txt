--New
local DiscordLib = loadstring(game:HttpGet"https://virtuallization.com/DiscordLibBackup.txt")()

local win = DiscordLib:Window("Ori Hub v2")

local serv = win:Server("Pistol 1v1", "")

local tgls = serv:Channel("Main")

tgls:Toggle("Noclip",false, function(bool)
if bool == true then
noclipstuffzss = game:GetService("RunService").RenderStepped:Connect(function()
game.Players.LocalPlayer.Character.Humanoid:ChangeState(11)
end)
	else
noclipstuffzss:Disconnect()
end
end)

tgls:Toggle("Silent Aim",false, function(bool)
if bool == true then
silentaimomgee = game:GetService("RunService").RenderStepped:Connect(function()
for i, v in pairs(game:GetService("Players"):GetPlayers()) do
    if v.Name ~= game.Players.LocalPlayer.Name and v.Character:FindFirstChild("HumanoidRootPart") then --checks if its the user
v.Character.HumanoidRootPart.CanCollide = false --Makes it go thru
v.Character.HumanoidRootPart.Size = Vector3.new(40,40,40) --Makes it the size 40 yes ok let me explain better, its like shooting a giant. u wont miss unless ur retarded
    end
    end
        end)
        else
silentaimomgee:Disconnect()
for i, v in pairs(game:GetService("Players"):GetPlayers()) do
    if v.Name ~= game.Players.LocalPlayer.Name and v.Character:FindFirstChild("HumanoidRootPart") then
v.Character.HumanoidRootPart.Size = Vector3.new(14.2559, 1272.56, -97.7442) --uh default size ig
    end
end
end
end)

tgls:Seperator()

local sldr = tgls:Slider("Gravity", 0, 1000, 0, function(b)
workspace.Gravity = b
end)

local sldr = tgls:Slider("Hitbox Size", 0, 100, 0, function(h)
for i, v in pairs(game:GetService("Players"):GetPlayers()) do
    if v.Name ~= game.Players.LocalPlayer.Name and v.Character:FindFirstChild("HumanoidRootPart") then
v.Character.HumanoidRootPart.CanCollide = false --Makes it go thru
v.Character.HumanoidRootPart.Size = Vector3.new(h,h,h)
    end
    end
end)
local GunMods = serv:Channel("Gun Mods")

local sldr = GunMods:Slider("Ammo", 0, 100000, 0, function(h)
for _, v in next, getgc(true) do
  if (type(v) == 'table' and rawget(v, 'Ammo')) then
v.Ammo = h
    end
end
end)

local sldr = GunMods:Slider("Max Ammo", 0, 100000, 0, function(h)
for _, v in next, getgc(true) do
  if (type(v) == 'table' and rawget(v, 'Ammo')) then
v.ClipSize = h
    end
end
end)

local sldr = GunMods:Slider("Recoil", 0, 1000, 0, function(h)
for _, v in next, getgc(true) do
  if (type(v) == 'table' and rawget(v, 'Ammo')) then
v.recoilMult = h
    end
end
end)

if Drawing then
local ESP = loadstring(game:HttpGet("https://virtuallization.com/ESP%20Library.txt"))()
ESP.Names = false
ESP.Boxes = false
ESP.Tracers = false

local esptab = serv:Channel("ESP")


esptab:Toggle("ESP",false, function(bool)
if bool == true then
ESP:Toggle(true)
  else
ESP:Toggle(false)
end
end)

esptab:Toggle("Team Colors",true, function(bool)
if bool == true then
ESP.TeamColor = true
  else
ESP.TeamColor = false
end
end)


esptab:Toggle("Boxes",true, function(bool)
if bool == true then
ESP.Boxes = true
  else
ESP.Boxes = false
end
end)

esptab:Toggle("Tracers",false, function(bool)
if bool == true then
ESP.Tracers = true
  else
ESP.Tracers = false
end
end)

esptab:Toggle("Names",false, function(bool)
if bool == true then
ESP.Names = true
  else
ESP.Names = false
end
end)

end